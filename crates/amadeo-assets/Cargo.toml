[package]
name = "amadeo-assets"
description = "Asset identity, sidecar metadata, and the catalogue that maps a declared id to a file."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
amadeo-ecs.workspace = true
# Background file reads (ADR 0041). The pool is passed in by the caller rather than owned here --
# one pool per process, not one per subsystem.
amadeo-jobs.workspace = true
thiserror.workspace = true

[lints]
workspace = true
