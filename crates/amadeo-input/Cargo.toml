[package]
name = "amadeo-input"
description = "Action-based input with deterministic sampling, recording, and replay."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
amadeo-core.workspace = true
amadeo-ecs.workspace = true
# `InputState` is a `Resource`, and ADR 0027 makes every resource reflectable. `ActionId` is a map
# key in it, so it implements `ReflectKey` too.
amadeo-reflect.workspace = true
thiserror.workspace = true

[lints]
workspace = true
